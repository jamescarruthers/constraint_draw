{
  "name": "constraint-draw",
  "version": "1.0.0",
  "description": "2D parametric constraint sketch solver",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
